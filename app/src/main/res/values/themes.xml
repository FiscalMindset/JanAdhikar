<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose owns all theming; this exists only for the cold-start window.
         Black splash: the app may be opened discreetly in a hostile situation. -->
    <style name="Theme.Janadhikar" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>
</resources>
